# Base URL of the API. The default matches the backend template running locally
# (apps/backend: pnpm quickstart && pnpm dev) — point it at any API you like.
VITE_API_BASE_URL=http://localhost:3000
VITE_APP_NAME=Dashboard
